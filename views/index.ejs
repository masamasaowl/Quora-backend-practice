<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora posts</title>

    <!-- for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <!-- bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <!-- css -->
    <link rel="stylesheet" href="/styleForIndex.css">
</head>
<body>
    <div class="navbar">
        <h1>
            <p class="Quora">Quora</p> posts
        </h1>

        <div class="createBtn">
            <a href="http://localhost:8080/posts/new">Create<i class="fa-solid fa-plus"></i> </a>
        </div>
    </div>


    <!-- a for of loop to display all the posts -->
    <% for(let post of posts){ %>
        <div class="container">
            <div class="post">
                <h3 class="user">@<%= post.username %></h3>
                <h4 class="content"><%= post.content %></h4>

                <!-- to see in detail we add the id -->
                <a class="btn" href="http://localhost:8080/posts/<%= post.id %>"> <i class="fa-solid fa-eye"></i></a>

                <!-- to edit the post we add a button -->
                <a class="btn editBtn" href="http://localhost:8080/posts/<%= post.id %>/update"> <i class="fa-solid fa-pen-to-square"></i></a>


                <!-- now the delete button is inside  a form so the action can be set to DELETE request -->
                <!-- form is only there to execute the DELETE request nothing else -->
                <form method="post" action="/posts/<%= post.id %>/delete?_method=DELETE" class="deleteForm" >

                    <!-- the delete button -->
                    <button class="btn deleteBtn">
                        <i class="fa-solid fa-trash"></i>
                    </button>
                </form>
            </div>
        </div>
    <% } %>

    <footer>
        <p>Created with <i class="fa-solid fa-heart"></i> & kind help of Shraddha ma'am</p>
    </footer>
</body>
</html>